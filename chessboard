function chessboard( size ) {
  for ( var i = 1; i <= size; i++ ) {
    var row = '';
    for ( var k = 1; k <= size; k++ ) {
      if ( (i + k) % 2 === 0 ) {
        row += ' ';
      } else {
        row += '#';
      }
    }
    console.log(row);
  }
}

chessboard(8);
